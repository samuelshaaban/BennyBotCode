#pragma once
#include "pins.h"

class Filesystem {
  public:
  Filesystem();
  void setup();

  // need some functions to read text or attributes from config files
  // need some way of reading and returning a usable media file
};